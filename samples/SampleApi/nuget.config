<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <!-- Local source: 'dotnet pack' the analyzer here (see README) alongside nuget.org. -->
    <add key="migrationlint-local" value="packages" />
  </packageSources>
</configuration>
